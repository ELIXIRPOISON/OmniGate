meta {
  name: create-policy
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/admin/v1/policies
  body: json
  auth: inherit
}

body:json {
  {
    "name": "burst",
    "windowSeconds": 60,
    "maxRequests": 500
  }
}
